let resultado=Math.PI;

/* Aproximacion por redondeo */
resultado = Math.round(2.8);
/* 0,5 redondea hacia arriba */
resultado = Math.round(2.5);


/* Redondea todo hacia arriba */
resultado=Math.ceil(2.4);

/* Redondea todo hacia abajo */
resultado = Math.floor(2.7);

/* Raiz Cuadrada */
resultado = Math.sqrt(16);

/* Valor absoluto */

resultado = Math.abs(-500);

/* Valor de potencia */

resultado = Math.pow(4,3);

/* El minimo */

resultado = Math.min(2,3,4,5,-6);

/* El maximo */
resultado = Math.max(2,3,4,5,-6);

/* Nmero random */

resultado = Math.random();


console.log( Math.round( resultado*30));

