const producto = "Monitor 20 pulgadas";

/* Conocer la cantidad de las letras de cadena de texto */
console.log(producto.length);

/* Es para encontrar la posicion donde empieza la cadena especificada */
console.log(producto.indexOf('Monitor'));

/* Busca la palabra especificada respetando mayusculas y minusculas */
console.log(producto.includes('monitor'));